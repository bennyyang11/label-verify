# Anthropic API key — required for label vision extraction.
# Get one at https://console.anthropic.com/  →  copy to .env.local (never commit the real key)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Optional: override the vision model (defaults to a current Claude vision model in code).
# ANTHROPIC_MODEL=
